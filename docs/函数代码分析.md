# FD2.EXE 已分析函数代码列表

## 📋 说明

本文档包含通过IDA Pro MCP Server分析的所有函数的C代码和注释。
所有代码均为反编译后的伪代码,经过人工整理和注释。

---

## 🎮 游戏逻辑函数

### 1. start() - 程序入口

**地址**: 0x3CCB4  
**大小**: 0x268 (616字节)

**功能**: DOS程序初始化入口,设置命令行参数和环境变量

```c
// write access to const memory has been detected, the output may be wrong!
void __noreturn start()
{
  int n129;                    // edi: 命令行参数计数器
  _BYTE *dst_1;                // edx: 目标字符串指针
  _BOOL1 v2;                   // zf: 零标志
  int v3;                      // ecx: 计数器
  const void *src;             // esi: 源字符串指针
  _BYTE *dst;                  // edi: 目标指针
  int __DS__;                  // edx: DS段寄存器
  unsigned int count;          // ecx: 复制计数
  _BYTE *__SS__;               // edi: SS段指针
  int v9;                      // esi: 循环计数器
  __int16 v10;                 // bp: 参数计数器
  _BYTE *v11;                  // esi: 字符串指针
  void *v12;                   // eax: 内存指针
  int inited;                  // eax: 初始化结果
  
  _enable();                   // 启用中断
  dword_52818 = (int)&retaddr; // 保存返回地址
  dword_52804 = (int)&retaddr; // 保存返回地址
  LOWORD(dword_52810) = 36;    // 设置段值
  
  // DOS中断: 获取DOS版本
  __asm { int     21h; DOS - GET DOS VERSION }
  n12288 = 12288;
  n129 = 129;
  
  // DOS中断: OEM功能
  __asm { int     21h; DOS - DOS v??? - OEM FUNCTION }
  
  n9_0 = 0;
  _DS___2 = __DS__;
  *(_DWORD *)byte_52834 = 0;
  *(_WORD *)&byte_52834[4] = MEMORY[0x26C];
  
  dst_1 = &unk_546BF;
  v2 = 1;
  v3 = MEMORY[0x2C0];
  
  // 解析命令行参数,跳过前导空格
  do
  {
    if ( !v3 )
      break;
    v2 = *MK_FP(dword_52810, n129++) == 32;  // 检查是否为空格
    --v3;
  }
  while ( v2 );
  
  src = (const void *)(n129 - 1);
  LOBYTE(dst_1) = (unsigned __int8)&unk_546BF & 0xF0;
  dst = dst_1;
  __DS__ = __DS__;
  
  // 复制命令行参数到缓冲区
  if ( !v2 )
  {
    count = v3 + 1;
    qmemcpy(dst, src, count);
    dst += count;
  }
  *dst = 0;
  __SS__ = dst + 1;
  *__SS__ = 0;
  
  v9 = 0;
  __SS___1 = (int)__SS__;
  __DS__ = *(_WORD *)&byte_52834[4];
  v10 = 0;
  
  // 解析环境变量,查找"no8mag=1"参数
  while ( 1 )
  {
    if ( (*MK_FP(*(_WORD *)&byte_52834[4], v9) | 0x20202020) == 0x37386F6E  // "no8g"
      && *MK_FP(*(_WORD *)&byte_52834[4], v9 + 4) == 61 )                    // "="
    {
      ++v10;
    }
    do
      v2 = *MK_FP(*(_WORD *)&byte_52834[4], v9++) == 0;
    while ( !v2 );
    
    if ( !*MK_FP(*(_WORD *)&byte_52834[4], v9) )
    {
      v11 = (_BYTE *)(v9 + 3);
      do
      {
        v2 = *v11 == 0;
        *MK_FP(__DS__, __SS__++) = *v11++;
      }
      while ( !v2 );
      
      word_52830 = v10;
      _SS___0 = (int)__SS__;
      dword_52800 = (int)&retaddr;
      LOBYTE(__DS__) = &unk_546B0 - (_UNKNOWN *)&buf__3;
      memset(&buf__3, 0, 4 * ((unsigned int)(&unk_546B0 - (_UNKNOWN *)&buf__3) >> 2));
      
      v12 = &unk_546BF;
      LOBYTE(v12) = (unsigned __int8)&unk_546BF & 0xF0;
      dword_52808 = (int)v12;
      _SS___2 = __SS___1;
      
      inited = _InitRtns();    // 初始化运行时
      _CMain(inited, __DS__);  // 调用C主函数
    }
  }
}
```

---

### 2. main() - 游戏主循环

**地址**: 0x25BF4  
**大小**: 0x2C7 (711字节)

**功能**: 游戏主循环,处理状态机和场景切换

```c
int __cdecl main(int argc, const char **argv, const char **envp)
{
  __int32 v3;                   // eax
  int v4;                       // edx
  int n16;                      // ecx: 场景ID
  int n80;                      // ebx: 数据指针
  int v7;                       // eax
  int v8;                       // eax
  int v9;                       // eax
  int v10;                      // edx
  int v11;                      // eax
  int v12;                      // edi
  int i;                        // esi: 循环/状态变量
  int v14;                      // eax: 状态值
  int v15;                      // eax: 状态处理结果
  int v16;                      // edx
  int v17;                      // edi: 退出标志
  
  // 初始化游戏系统
  sub_3702F(v3, v4, n80, n16, 28);
  v7 = sub_37D3E();
  sub_3AA72(v7);
  
  // 加载游戏数据
  dword_53ED8 = v8;
  if ( v8 )
  {
    n16_0 = 1;
    sub_3ACA3(v8, v8);
    dword_53ED0 = v8;
  }
  
  v9 = sub_3908B(v8);
  v10 = v9;
  dword_53EDC = v9;
  if ( v9 )
  {
    byte_53EF1 = 1;
    sub_392D0(v9, v9, n80, n16, v9);
    dword_53EE4 = v11;
    sub_392D0(v11, v10, n80, n16, dword_53EDC);
    dword_53EE8 = v9;
  }
  
  // 加载数据文件
  FDOTHER_DAT__2 = (int)sub_111BA(v9, v10, n80, n16, 
                                   (int)"FDOTHER.DAT", FDOTHER_DAT__2, 31);
  FDOTHER_DAT__3 = (int)sub_111BA(FDOTHER_DAT__2, v10, n80, n16, 
                                   (int)"FDOTHER.DAT", FDOTHER_DAT__3, 1);
  FDOTHER_DAT__4 = (int)sub_111BA(FDOTHER_DAT__3, v10, n80, n16, 
                                   (int)"FDOTHER.DAT", FDOTHER_DAT__4, 2);
  FDOTHER_DAT__5 = (int)sub_111BA(FDOTHER_DAT__4, v10, n80, n16, 
                                   (int)"FDOTHER.DAT", FDOTHER_DAT__5, 3);
  FDOTHER_DAT__6 = (int)sub_111BA(FDOTHER_DAT__5, v10, n80, n16, 
                                   (int)"FDOTHER.DAT", FDOTHER_DAT__6, 4);
  FDOTHER_DAT__7 = (int)sub_111BA(FDOTHER_DAT__6, v10, n80, n16, 
                                   (int)"FDOTHER.DAT", FDOTHER_DAT__7, 5);
  FDTXT_DAT__0 = (int)sub_111BA(FDOTHER_DAT__7, v10, n80, n16, 
                                 (int)"FDTXT.DAT", FDTXT_DAT__0, 0);
  FDOTHER_DAT__8 = (int)sub_111BA(FDTXT_DAT__0, v10, n80, n16, 
                                   (int)"FDOTHER.DAT", FDOTHER_DAT__8, 6);
  
  // 分配内存
  n8_0 = malloc(32);
  n655360 = malloc(655360 + 1);  // 屏幕缓冲区
  n8_3 = malloc(2560);           // 场景数据
  
  // 设置显示模式 (VGA模式13h)
  n3 = 19;
  int386(16, &n3, &n3);  // BIOS视频中断
  
  dword_53C0F = MEMORY[0x46C];  // 保存定时器值
  
  // 初始化随机数种子
  v12 = ((int (__cdecl *)())rand)() % 256;
  for ( i = 0; i < v12; ++i )
    sub_4EBE3();  // 随机数生成器
  
  // 主游戏循环
  while ( 1 )
  {
    v14 = sub_25977(18, 0);     // 获取游戏状态
    v15 = sub_25EBB(v14);       // 处理游戏状态
    v16 = v15;
    v17 = v15;
    
    if ( v15 )
    {
      if ( v15 == -1 )          // 退出游戏
        v17 = 0;
    }
    else
    {
      // 状态0: 主循环处理
      do
      {
        i = sub_117E7(v16, n80, i);  // 处理输入
        
        if ( n2_0 == 1 )          // 状态1: 初始化场景
        {
          byte_51AAC = 0;
          sub_22E5C();            // 初始化场景
          byte_51AAC = 1;
          n2_0 = 0;
          i = 1;
        }
        else if ( n2_0 == 2 )     // 状态2: 主游戏场景
        {
          byte_51AAC = 0;
          sub_25977(-1, 1);
          
          // 调用场景初始化函数
          ((void (__usercall *)(unsigned __int8 *@<edi>))funcs_25E23[n17])((unsigned __int8 *)v17);
          
          i = sub_26152();        // 场景逻辑处理
          
          if ( i )                // 场景结束
          {
            v17 = 1;
          }
          else
          {
            // 调用场景结束处理函数
            ((void (__usercall *)(unsigned __int8 *@<edi>))funcs_25E3A[n17])((unsigned __int8 *)v17);
            sub_25977((unsigned __int8)byte_51E63[n17], 0);
          }
          
          byte_51AAC = 1;
          n2_0 = 0;
          sub_4E381();            // 清理
        }
      }
      while ( !i );
      
      if ( i == -1 )
        v17 = 1;
    }
    
    // 检查退出条件
    if ( v17 )
    {
      sub_37ED8();                // 退出清理
      n3 = 3;
      int386(16, &n3, &n3);      // 恢复文本模式
      JUMPOUT(0x16F04);          // 退出程序
    }
  }
}
```

---

### 3. sub_25EBB() - 游戏状态处理

**地址**: 0x25EBB  
**大小**: 0x297 (663字节)

**功能**: 处理游戏状态转换和场景加载

```c
bool __usercall sub_25EBB@<eax>(
    __int32 a1@<eax>, 
    int a2@<edx>, 
    int a3@<ecx>, 
    int n99@<ebx>, 
    unsigned __int8 *a5@<edi>)
{
  int v5;                        // eax
  __int32 v6;                    // eax: 状态值
  int v7;                        // eax
  int v8;                        // eax
  int v9;                        // eax
  unsigned __int8 *v10;          // ebx: 存档数据指针
  unsigned __int8 *v11;          // edi
  __int64 v12;                   // rax
  int v13;                       // esi
  int v14;                       // eax
  bool v15;                      // esi: 结果标志
  int v16;                       // ebx
  __int32 v18;                   // eax
  
  v5 = sub_3702F(a1, a2, n99, a3, 32);
  sub_1F894(v5, a2, n99, a3);
  
  // 状态0: 初始化
  if ( !v6 )
  {
    v7 = sub_1F882();
    n17 = 0;
    
    // 加载FDOTHER.DAT索引0
    FDOTHER_DAT = (int)sub_111BA(v7, a2, n99, a3, 
                                  (int)"FDOTHER.DAT", FDOTHER_DAT, 0);
    
    n16_1 = 0;
    byte_51AAC = 0;
    
    // 调用场景初始化函数
    ((void (__usercall *)(unsigned __int8 *@<edi>))funcs_25E3A[n17])(a5);
    sub_25977((unsigned __int8)byte_51E63[n17], a2, n99, a3, 
              (unsigned __int8)byte_51E63[n17], 0);
    
    byte_51AAC = 1;
    sub_4E381();
    return 0;
  }
  
  // 状态1: 退出场景
  if ( v6 != 1 )
  {
    sub_25977(v6, a2, n99, a3, -1, 0);
    sub_10010(v18, a2, a3, n99, a5);
    sub_25977((unsigned __int8)byte_51E63[n17], a2, n99, a3, 
              (unsigned __int8)byte_51E63[n17], 0);
    return 0;
  }
  
  // 状态1: 加载游戏存档
  FDOTHER_DAT__11 = (int)sub_111BA(1, a2, n99, a3, 
                                    (int)"FDOTHER.DAT", FDOTHER_DAT__11, 13);
  v8 = sub_1F882();
  FDOTHER_DAT = (int)sub_111BA(v8, a2, n99, a3, 
                                (int)"FDOTHER.DAT", FDOTHER_DAT, 0);
  
  // 清零缓冲区
  v9 = memset(n99, 655360, 0, 64000);
  sub_11D40(v9, a2, n99, a3, 0, 255, 0);
  
  // 分配存档数据内存 (22987字节)
  v10 = (unsigned __int8 *)malloc(22987);
  v11 = v10;
  
  // 读取存档文件 FD2.SAV
  v12 = fopen("FD2.SAV", "rb");
  v13 = v12;
  if ( (_DWORD)v12 )
  {
    sub_373CA(v10, 1u, 22987, v12);  // 读取22987字节
    sub_4DF28((char *)v10, 22987);   // 解密/处理数据
    fclose(v13);
  }
  else
  {
    // 存档不存在,填充255
    memset((int)v10, (int)v10, 255, 22987);
  }
  
  n4_1 = 0;
  
  // 解析存档数据
  do
  {
    sub_29BCB((int)v11, 0);
    v15 = v14;
    
    if ( v14 != -1 )
    {
      // 提取场景数据
      v16 = (int)&v11[2600 * (_DWORD)n4_1 + 12587];
      v12 = memmove(n8_3, v16, 2560);  // 复制2560字节场景数据
      
      v10 = (unsigned __int8 *)(v16 + 2560);
      n17 = *v10;           // 场景ID
      n16_1 = v10[1];       // 子场景ID
      n999_0 = *(_DWORD *)(v10 + 2);
      byte_51AAB = v10[6];
      byte_53AF9 = v10[7];
      n127 = v10[8];
      byte_51E62 = v10[9];
      
      if ( n17 == 255 )     // 场景ID 255表示结束
        v15 = 0;
    }
    
    sub_26996();
  }
  while ( !v15 );
  
  free(v11);
  free(FDOTHER_DAT__11);
  FDOTHER_DAT__11 = 0;
  
  if ( v15 )
  {
    byte_51AAC = 0;
    v15 = sub_26152();      // 处理场景逻辑
    
    if ( !v15 )
    {
      // 调用场景函数
      ((void (__usercall *)(unsigned __int8 *@<edi>))funcs_25E3A[n17])(v11);
      sub_25977((unsigned __int8)byte_51E63[n17], SHIDWORD(v12), (int)v10, a3, 
                (unsigned __int8)byte_51E63[n17], 0);
    }
    
    byte_51AAC = 1;
  }
  
  sub_4E381();
  return v15;
}
```

---

### 4. sub_117E7() - 键盘输入处理

**地址**: 0x117E7  
**大小**: 0x2C1 (705字节)

**功能**: 处理键盘输入,映射扫描码到游戏动作

```c
int __usercall sub_117E7@<eax>(
        int a1@<edx>,
        int n80_1@<ebx>,
        int a3@<esi>,
        __int32 a4@<eax>,
        int a5@<ecx>,
        unsigned __int8 *a6@<edi>)
{
  int n44;                     // eax: 按键扫描码
  int v7;                      // ecx
  int v8;                      // ebx
  int n6;                      // esi: 循环计数器
  int v10;                     // eax
  int n6_2;                    // eax
  int n6_1;                    // esi
  int v14;                     // eax
  int v15;                     // ebx
  _BYTE *v16;                  // eax: 项目数据指针
  int n2;                      // edx: 项目类型
  int n10;                     // ebx: 项目状态
  int n3_1;                    // eax
  int n3;                      // edx
  int v21;                     // eax
  
  sub_3702F(a4, a1, n80_1, a5, 24);
  
  // 获取按键扫描码
  n44 = sub_11AA8();
  
  // 处理系统按键 (ESC=1, F10=44, F12=76)
  if ( n44 == 1 || n44 == 44 || n44 == 76 )
  {
    v7 = 0;
    v8 = dword_53AE9;
    
    // 遍历处理所有项目
    for ( n6 = 0; n6 < n6_0; ++n6 )
    {
      v10 = n8_1 + 80 * v8;
      
      // 检查项目状态: 未选中且类型为2
      if ( (*(_BYTE *)(v10 + 5) & 0x85) == 0 && *(_BYTE *)(v10 + 6) == 2 && !v7 )
      {
        sub_12D7B(v8);         // 处理项目
        dword_53AE9 = v8 + 1;  // 更新当前项
        
        if ( v8 + 1 == n6_0 )
          dword_53AE9 = 0;     // 循环回第一个
        
        v7 = 1;
      }
      
      if ( ++v8 == n6_0 )
        v8 = 0;
    }
    
    sub_4E381();
    return 0;
  }
  
  // 处理特殊按键
  if ( n44 != 57 && n44 != 28 )  // 不是空格或回车
  {
    if ( n44 != 34 )             // 不是'2'键
    {
      switch ( n44 )
      {
        case ';':               // 'I'键: 信息
        case 'I':
          sub_2000A();
          return 0;
          
        case '<':               // 'G'键: 交互
        case 'G':
          n3_1 = sub_12C0D();   // 获取当前选择项
          n3 = n3_1;
          
          if ( n3_1 != -1 )
          {
            v21 = 80 * n3_1 + n8_1;
            
            // 检查项目是否可交互
            if ( *(_BYTE *)(v21 + 7) != 121 && *(_BYTE *)(v21 + 31) != 10 )
            {
              sub_17AED(n3, a3);  // 执行交互
              return 0;
            }
          }
          break;
          
        case 'H':               // 上方向键 (72)
          sub_25A96(72, a1, n80_1, a5, FDOTHER_DAT__2, 0, 1);
          sub_11B48();
          return 0;
          
        case 'P':               // 下方向键 (80)
          sub_25A96(80, a1, n80_1, a5, FDOTHER_DAT__2, 0, 1);
          sub_11B9B();
          return 0;
          
        case 'K':               // 左方向键 (75)
          sub_25A96(75, a1, n80_1, a5, FDOTHER_DAT__2, 0, 1);
          sub_11C59();
          return 0;
          
        case 'M':               // 右方向键 (77)
          sub_25A96(77, a1, n80_1, a5, FDOTHER_DAT__2, 0, 1);
          sub_11BFA();
          break;
      }
    }
    return 0;
  }
  
  // 处理确认键 (回车=28 或 空格=57)
  if ( byte_51A42 )
    --byte_51A42;
  
  n6_2 = sub_12C0D();          // 获取当前选择项
  n6_1 = n6_2;
  
  if ( n6_2 != -1 )
  {
    v16 = (_BYTE *)(n8_1 + 80 * n6_2);  // 获取项目数据
    n2 = (unsigned __int8)v16[6];       // 项目类型
    n999_2 = 0;
    
    // 检查项目标志
    if ( v16[7] != 121 )
    {
      n10 = (unsigned __int8)v16[31];   // 项目状态
      
      if ( n10 != 10 )
      {
        // 类型2且状态正常的可交互项目
        if ( n2 == 2 && (char)v16[5] >= 0 && !v16[38] )
        {
          sub_25A96(0, 2, n10, a5, FDOTHER_DAT__2, 7, 1);
          while ( !sub_18890(n6_1) )    // 等待动画播放
            ;
        }
        else
        {
          sub_17AED(n6_1, a3);          // 执行交互
        }
        
        sub_11CAC(0);
        sub_1E292(a6, n6_1);
        funcs_1197B[n17]();             // 场景回调
        sub_13565();
        
        if ( n255 != 255 )
          ((void (__usercall *)(unsigned __int8 *@<edi>))funcs_1199C[n255])(a6);
        
        n255 = 255;
        goto LABEL_15;
      }
    }
    return 0;
  }
  
  // 处理其他情况
  do
  {
    v14 = sub_16F55();
    v15 = v14;
  }
  while ( !v14 );
  
  if ( v14 == 1 )
    return 0;
  
  return v15;

LABEL_15:
  sub_4E381();
  return 0;
}
```

---

### 5. sub_26152() - 场景主逻辑

**地址**: 0x26152  
**大小**: 0x49A (1178字节)

**功能**: 场景主循环,处理用户交互和场景切换

```c
bool __usercall sub_26152@<eax>(
    __int32 a1@<eax>, 
    int a2@<edx>, 
    int n8@<ecx>, 
    int a4@<ebx>, 
    int a5@<ebp>)
{
  __int64 _rb__1;               // rax: 文件指针
  int _rb_;                     // esi: 文件句柄
  int n16;                      // ebx: 循环计数器
  int v8;                       // eax
  int v9;                       // ebx
  __int64 _FDSHAP.DAT_;         // rax: 形状数据
  __int16 *_FDOTHER.DAT_;       // esi: 其他数据
  int v13;                      // ebx
  __int32 v14;                  // eax
  int v15;                      // edx
  __int32 n3;                   // eax
  char v17;                     // al: 结果
  __int16 v18;                  // [esp+4h] [ebp-18h]
  char v19;                     // [esp+6h] [ebp-16h]
  unsigned __int8 v20;          // [esp+7h] [ebp-15h] BYREF
  char v21;                     // [esp+8h] [ebp-14h]: 退出标志
  unsigned __int8 n16_1;        // [esp+Ch] [ebp-10h]: 按键计数
  int v23;                      // [esp+18h] [ebp-4h]
  
  sub_3702F(a1, a2, a4, n8, 68);
  v23 = a4;
  v21 = 0;
  v18 = unk_52405;
  v19 = unk_52407;
  n16_1 = 0;
  
  // 释放旧内存
  if ( n8_1 )
    free(n8_1);
  n8_1 = 0;
  
  if ( FDFIELD_DAT__1 )
    free(FDFIELD_DAT__1);
  FDFIELD_DAT__1 = 0;
  
  if ( FDSHAP_DAT )
    free(FDSHAP_DAT);
  FDSHAP_DAT = 0;
  
  if ( FDFIELD_DAT__0 )
    free(FDFIELD_DAT__0);
  FDFIELD_DAT__0 = 0;
  
  if ( dword_53A61 )
    free(dword_53A61);
  
  // 打开FDICON.B24图标文件
  _rb__1 = fopen("FDICON.B24", "rb");
  _rb_ = _rb__1;
  dword_53BDF = 0;
  
  // 加载图标数据
  for ( n16 = 0; n16 < ::n16_1; ++n16 )
  {
    n8 = n8_3;
    sub_11019(
      *(unsigned __int8 *)(n8_3 + 80 * n16 + 7),
      SHIDWORD(_rb__1),
      n16,
      n8_3,
      *(unsigned __int8 *)(n8_3 + 80 * n16 + 7),
      _rb_);
  }
  fclose(_rb_);
  
  // 检查场景标志
  if ( byte_523E7[n17] )
  {
    LODWORD(_rb__1) = memset(n16, 655360, 0, 64000);
    sub_11D40(_rb__1, SHIDWORD(_rb__1), n16, n8, 0, 255, 0);
    sub_1956B(75);
    
    // 渲染文本
    sub_15F84(&v20, FDTXT_DAT__0, 410, 693540, 320, 205, 76, 74, 19, 1);
    sub_16559(0);
    FDFIELD_DAT__0 = 1;
    sub_19953();                // 场景渲染循环
    v9 = v8;
    FDFIELD_DAT__0 = 0;
    sub_197E5();
    LODWORD(_rb__1) = sub_26996();
    
    if ( v9 != -1 && !n4_1 )
    {
      FDOTHER_DAT__11 = (int)sub_111BA(_rb__1, SHIDWORD(_rb__1), v9, n8, 
                                        (int)"FDOTHER.DAT", FDOTHER_DAT__11, 13);
      sub_2968D(0);
      free(FDOTHER_DAT__11);
    }
    
    // 等待场景加载完成
    do
    {
      n8_1 = n8_3;
      LODWORD(_rb__1) = sub_2AF28();
      n8_1 = 0;
    }
    while ( !(_DWORD)_rb__1 );
    
    sub_11D40(_rb__1, SHIDWORD(_rb__1), v9, n8, 0, 255, 255);
    return 0;
  }
  
  // 加载形状数据
  _FDSHAP.DAT_ = malloc(153216);
  FDSHAP_DAT = _FDSHAP.DAT_;
  
  dword_53F56 = (int)sub_4E809(n17);
  LOBYTE(n16) = *(_BYTE *)dword_53F56;
  LODWORD(_FDSHAP.DAT_) = sub_1F882(dword_53F56, SHIDWORD(_FDSHAP.DAT_), n16, n8);
  sub_25977(_FDSHAP.DAT_, SHIDWORD(_FDSHAP.DAT_), n16, n8, 10, 0);
  
  n5 = 0;
  
  // 加载FDOTHER.DAT
  _FDOTHER.DAT_ = (__int16 *)sub_111BA(
                               *((unsigned __int8 *)&v18 + (unsigned __int8)n16),
                               SHIDWORD(_FDSHAP.DAT_),
                               n16,
                               n8,
                               (int)"FDOTHER.DAT",
                               0,
                               *((unsigned __int8 *)&v18 + (unsigned __int8)n16));
  
  sub_4E98D(_FDOTHER.DAT_, 0, 0, FDSHAP_DAT + 32904, 456, -1);
  LODWORD(_FDSHAP.DAT_) = free(_FDOTHER.DAT_);
  FDOTHER_DAT__12 = 0;
  FDOTHER_DAT__12 = (int)sub_111BA(_FDSHAP.DAT_, SHIDWORD(_FDSHAP.DAT_), n16, n8, 
                                    (int)"FDOTHER.DAT", 0, 10);
  
  LODWORD(_FDSHAP.DAT_) = sub_265EC(&v20);
  sub_1F525(_FDSHAP.DAT_, SHIDWORD(_FDSHAP.DAT_), n16, n8);
  sub_4E381();
  
  // 主场景循环
  do
  {
    sub_265EC(&v20);
    v13 = MEMORY[0x46C];       // 获取定时器值
    
    while ( !sub_10620() )     // 等待键盘输入
    {
      // 每4个滴答更新一次
      if ( (unsigned int)(MEMORY[0x46C] - (__int16)v13) >= 4 )
      {
        if ( ++n3_4 == 4 )
          n3_4 = 0;
        sub_265EC(&v20);
        LOWORD(v13) = MEMORY[0x46C];
      }
    }
    
    // 获取按键扫描码
    HIBYTE(::n3) = 16;
    v14 = int386(22, &::n3, &::n3);
    v15 = HIBYTE(::n3);
    
    switch ( HIBYTE(::n3) )
    {
      case 0xE0u:              // 扩展键前缀
      case 0x52u:              // Insert
        HIBYTE(::n3) = 28;     // 转换为回车
        break;
        
      case 0x22u:              // '2'键
        if ( ++n16_1 == 10 )
          n16_1 = 0;
        sub_25977(n16_1, 34, v13, n8, n16_1, 0);
        break;
        
      case 0x4Du:              // 右方向键 (M)
        sub_25A96(v14, 77, v13, n8, FDOTHER_DAT__2, 0, 1);
        if ( --n5 < 0 )
          n5 = 5;
        break;
        
      case 0x4Bu:              // 左方向键 (K)
        sub_25A96(v14, 75, v13, n8, FDOTHER_DAT__2, 0, 1);
        if ( ++n5 > 5 )
          n5 = 0;
        break;
        
      default:
        v13 = *(unsigned __int8 *)(dword_53F56 + 2);
        if ( __PAIR64__(HIBYTE(::n3), n5) == __PAIR64__(v13, *(unsigned __int8 *)(dword_53F56 + 1)) )
          n5 = 5;
        break;
    }
    
    n3 = HIBYTE(::n3);
    
    // 检查退出键 (回车或空格)
    if ( HIBYTE(::n3) != 28 )
    {
      n3 = (unsigned __int8)::n3;
      if ( (unsigned __int8)::n3 != 32 )
        continue;
    }
    
    if ( n5 != 2 )
      sub_25A96(n3, v15, v13, n8, FDOTHER_DAT__2, 1, 3);
    
    sub_2670E(a5, &v20);       // 执行场景特效
    v21 = v17;
  }
  while ( !v21 );
  
  free(FDOTHER_DAT__12);
  return n5 != 2;
}
```

---

## 🎨 绘制框架函数

### 6. sub_15F84() - 文本/UI渲染引擎

**地址**: 0x15F84  
**大小**: 0x564 (1380字节)

**功能**: 解析文本数据格式并渲染到屏幕,支持特殊标记和图片

```c
void __usercall sub_15F84(
        unsigned __int8 *a1@<edi>,
        __int32 a2@<eax>,
        int a3@<edx>,
        int a4@<ecx>,
        int a5@<ebx>,
        int arg0,               // 文本数据基址
        int arg4,               // 参数
        int n658255,           // 屏幕偏移 (初始658255)
        int argC,               // 行跨度 (320)
        char arg10,             // 前景色 (205)
        char arg14,             // 背景色 (76)
        int arg18,              // 标志 (74)
        int arg1C,              // 参数
        int arg20)              // 刷新标志 (19)
{
  int n658255_1;                // ebp: 当前屏幕偏移
  __int16 *v15;                 // esi: 文本数据指针
  int v16;                      // edx
  int n658255_2;                // eax
  int n39;                      // ebp: 图片索引
  __int32 v19;                  // eax
  int *v20;                     // eax: 精灵缓冲区
  __int32 v21;                  // eax
  unsigned __int8 *v22;         // edi
  int *v23;                     // eax: 精灵缓冲区
  int n10;                      // eax: 当前字符/标记
  __int32 v25;                  // eax
  unsigned __int8 *v26;         // edi
  int v27;                      // ebp
  __int32 v28;                  // eax
  unsigned __int8 *v29;         // edi
  int v30;                      // ebp
  _BYTE v31[12];                // [esp+0h] [ebp-34h] BYREF: 格式化缓冲区
  __int16 *v32;                 // [esp+Ch] [ebp-28h]: 文本指针副本
  int i;                        // [esp+10h] [ebp-24h]: 循环计数器
  int n2;                       // [esp+14h] [ebp-20h]: 精灵类型
  int *v35;                     // [esp+18h] [ebp-1Ch]: 精灵缓冲区
  int n3;                       // [esp+1Ch] [ebp-18h]: 层级计数
  unsigned __int8 v37;          // [esp+20h] [ebp-14h]: 字符串长度
  unsigned __int8 *v38;         // [esp+28h] [ebp-Ch]: 场景指针副本
  int v39;                      // [esp+30h] [ebp-4h]
  
  sub_3702F(a2, a3, a5, a4, 92);
  v39 = a5;
  v38 = a1;
  n3 = 0;                       // 初始化层级计数
  v35 = 0;                      // 精灵缓冲区为空
  n2 = 0;
  n658255_1 = n658255;
  
  // 获取文本数据指针
  v15 = (__int16 *)(*(__int16 *)(arg0 + 2 * arg4) + arg0);
  
  while ( 1 )
  {
    while ( 1 )
    {
      while ( 1 )
      {
        while ( 1 )
        {
          while ( 1 )
          {
            n10 = *v15;         // 获取当前字符/标记索引
            
            // 标记 -1: 文本结束
            if ( n10 == -1 )
            {
              if ( v35 )        // 清理精灵缓冲区
              {
                sub_16559(0);
                sub_16C57(0);
                sub_16B43(v35, n2);
                n1832 = 0;
              }
              JUMPOUT(0x15309);  // 返回
            }
            
            // 标记 -2: 换行
            if ( n10 == -2 )
            {
              if ( (n1832 == 1832 || n1832 == 36887) && n3 == 3 )
              {
                sub_16E24();
                --n3;
              }
              n658255_1 = ++n3 * arg1C * argC + n658255;
              goto LABEL_50;
            }
            
            // 标记 -3: 字体切换
            if ( n10 != -3 )
              break;
            
            if ( (n1832 == 1832 || n1832 == 36887) && n3 == 3 )
            {
              sub_16E24();
              --n3;
            }
            n658255_1 = ++n3 * arg1C * argC + n658255;
            ++v15;
            
            if ( n1832 == 1832 || n1832 == 36887 )
              sub_16559(0);
            sub_16C57(1);
            arg20 = 1;
          }
          
          v16 = (int)(v15 + 1);
          v32 = v15 + 1;
          
          // 标记 -4: 递归调用 (索引dword_53AD9)
          if ( n10 == -4 )
          {
            sub_15F84(a1, FDTXT_DAT__0, dword_53AD9, n658255_1, 
                     argC, 205, 76, 74, 19, 1);
            goto LABEL_13;
          }
          
          // 标记 -5: 递归调用 (索引dword_53ADD)
          if ( n10 != -5 )
            break;
          
          sub_15F84(a1, FDTXT_DAT__0, dword_53ADD, n658255_1, 
                   argC, 205, 76, 74, 19, 1);
          
LABEL_13:
          n658255_1 = n658255_2;
          v15 = v32;
        }
        
        // 标记 -6: 数字显示
        if ( n10 != -6 )
          break;
        
        sprintf(v31, "%d", n999_1);
        v37 = strlen(v31);
        
        for ( i = 0; v37 > i; ++i )
        {
          sub_4ED7A(FDOTHER_DAT__6, (unsigned __int8)v31[i] - 48, 
                   n658255_1, argC, arg10, arg14, arg18);
          if ( sub_10620() )
            arg20 = 0;
          if ( arg20 )
            sub_164E8();
          n658255_1 += 16;      // 每个字符16像素宽
        }
        
LABEL_50:
        ++v15;
      }
      
      // 标记 -17: 加载图片1 (偏移1832)
      if ( n10 == -17 )
      {
        if ( v35 )
        {
          sub_16559(0);
          sub_16C57(0);
          sub_16B43(v35, n2);
        }
        
        n1832 = 1832;
        n39 = (unsigned __int16)v15[1];
        v19 = sub_12C60(n39);
        
        if ( v19 == -1 )
          n2 = 0;
        else
          n2 = 2;
        
        if ( n39 != 39 )
        {
          a1 = (unsigned __int8 *)dword_53C1B;
          n39 = *(unsigned __int8 *)(dword_53C1B + 7);
        }
        
        DATO_DAT = (int)sub_111BA(v19, v16, a5, a4, 
                                   (int)"DATO.DAT", DATO_DAT, n39);
        v20 = sub_165AC(*a1, a1[1], n2);
        
LABEL_33:
        v35 = v20;
        a1 = (unsigned __int8 *)(*(unsigned __int8 *)DATO_DAT + DATO_DAT);
        sub_4EBFF((_BYTE *)(n1832 + 655360), (__int16 *)a1, 320);
        
        arg20 = 1;
        n3 = 0;
        n658255 = 658255;
        n658255_1 = 658255;
        goto LABEL_42;
      }
      
      // 标记 -18: 加载图片2 (偏移36887)
      if ( n10 != -18 )
        break;
      
      if ( v35 )
      {
        sub_16559(0);
        sub_16C57(0);
        sub_16B43(v35, n2);
      }
      
      n1832 = 36887;
      v21 = sub_12C60((unsigned __int16)v15[1]);
      
      if ( v21 == -1 )
        n2 = 0;
      else
        n2 = 112;
      
      v22 = (unsigned __int8 *)dword_53C1B;
      DATO_DAT = (int)sub_111BA(v21, v16, a5, a4, 
                                 (int)"DATO.DAT", DATO_DAT, 
                                 *(unsigned __int8 *)(dword_53C1B + 7));
      v23 = sub_165AC(*v22, v22[1], n2);
      
LABEL_41:
      v35 = v23;
      a1 = (unsigned __int8 *)(*(unsigned __int8 *)DATO_DAT + DATO_DAT);
      sub_4EC31((_BYTE *)(n1832 + 655360), (__int16 *)a1, 320);
      
      arg20 = 1;
      n3 = 0;
      n658255 = 693535;
      n658255_1 = 693535;
      
LABEL_42:
      v15 += 2;
    }
    
    // 标记 -19: 动态图片1
    if ( n10 == -19 )
    {
      if ( v35 )
      {
        sub_16559(0);
        sub_16C57(0);
        sub_16B43(v35, n2);
      }
      
      n1832 = 1832;
      v25 = 80 * (unsigned __int16)v15[1];
      v26 = (unsigned __int8 *)(v25 + n8_1);
      v27 = *(unsigned __int8 *)(v25 + n8_1 + 7);
      n2 = 2;
      
      DATO_DAT = (int)sub_111BA(v25, v16, a5, a4, 
                                 (int)"DATO.DAT", DATO_DAT, v27);
      v20 = sub_165AC(*v26, v26[1], 2);
      goto LABEL_33;
    }
    
    // 标记 -20: 动态图片2
    if ( n10 == -20 )
    {
      if ( v35 )
      {
        sub_16559(0);
        sub_16C57(0);
        sub_16B43(v35, n2);
      }
      
      n1832 = 36887;
      v28 = 80 * (unsigned __int16)v15[1];
      v29 = (unsigned __int8 *)(v28 + n8_1);
      v30 = *(unsigned __int8 *)(v28 + n8_1 + 7);
      n2 = 112;
      
      DATO_DAT = (int)sub_111BA(v28, v16, a5, a4, 
                                 (int)"DATO.DAT", DATO_DAT, v30);
      v23 = sub_165AC(*v29, v29[1], 112);
      goto LABEL_41;
    }
    
    // 默认: 渲染字符
    sub_4ED7A(FDOTHER_DAT__6, n10, n658255_1, argC, arg10, arg14, arg18);
    n658255_1 += 16;
    v15 = v32;
    
    if ( sub_10620() )
      arg20 = 0;
    if ( arg20 )
      sub_164E8();
  }
}
```

---

### 7. sub_4EBFF() - 正向位块传输

**地址**: 0x4EBFF

**功能**: 从左到右绘制图像 (标准BitBlt)

```c
void __cdecl sub_4EBFF(_BYTE *a1, __int16 *a2, int a3)
{
  __int16 v4;                   // bp: 图像宽度
  __int16 v5;                   // dx: 图像高度
  int v6;                       // ecx: x坐标计数器
  char v7;                      // al: 像素值
  _BYTE *v8;                    // [esp-24h] [ebp-24h]: 行起始指针
  
  v4 = *a2;                     // 获取图像宽度
  v5 = a2[1];                   // 获取图像高度
  HIWORD(v6) = 0;
  
  // 逐行绘制
  do
  {
    v8 = a1;
    LOWORD(v6) = v4;
    
    do
    {
      sub_4EC66();              // 获取下一个像素值
      *a1++ = v7;               // 从左到右写入像素
      --v6;
    }
    while ( v6 );
    
    a1 = &v8[a3];               // 移动到下一行
    --v5;
  }
  while ( v5 );
}
```

**参数说明**:
- `a1`: 目标缓冲区地址 (通常是VGA显存)
- `a2`: 源图像数据数组 [宽度, 高度, 像素数据...]
- `a3`: 目标行跨度 (通常320)

---

### 8. sub_4EC31() - 反向位块传输

**地址**: 0x4EC31

**功能**: 从右到左绘制图像 (实现水平镜像效果)

```c
void __cdecl sub_4EC31(_BYTE *a1, __int16 *a2, int a3)
{
  __int16 v4;                   // bp: 图像宽度
  __int16 v5;                   // dx: 图像高度
  int v6;                       // ecx: x坐标计数器
  char v7;                      // al: 像素值
  _BYTE *v8;                    // [esp-24h] [ebp-24h]: 行起始指针
  
  v4 = *a2;                     // 获取图像宽度
  v5 = a2[1];                   // 获取图像高度
  HIWORD(v6) = 0;
  
  // 逐行绘制 (反向)
  do
  {
    v8 = a1;
    LOWORD(v6) = v4;
    
    do
    {
      sub_4EC66();              // 获取下一个像素值
      *a1-- = v7;               // 从右到左写入像素 (镜像)
      --v6;
    }
    while ( v6 );
    
    a1 = &v8[a3];               // 移动到下一行
    --v5;
  }
  while ( v5 );
}
```

**与sub_4EBFF的区别**:
- `*a1--` 而不是 `*a1++`
- 实现水平镜像效果
- 使用相同的图像数据可以正向/反向绘制

---

### 9. sub_4ED7A() - 字符/精灵渲染

**地址**: 0x4ED7A

**功能**: 从FDOTHER.DAT加载字形数据并渲染16x16字符

```c
void __cdecl sub_4ED7A(
        int _FDOTHER.DAT_,      // FDOTHER.DAT数据基址
        int n10,                // 字符索引 (0-255)
        int n658255,           // 屏幕偏移
        unsigned __int16 argC,  // 行跨度
        char arg10,             // 前景色
        char arg14,             // 背景色
        int arg18)              // 标志
{
  int arg18_1;                  // eax
  __int16 arg18_2;              // bx
  int value;                    // eax: 填充值
  char *n658255_1;              // edi: 屏幕指针
  char n16;                     // bl: 高度计数器
  __int16 *v12;                 // esi: 字形数据指针
  char *n658255_2;              // edi: 行起始指针
  char arg10_1;                 // dl: 前景色副本
  char arg14_1;                 // dh: 背景色副本
  char n16_1;                   // ch: 高度计数器
  __int16 v17;                  // ax: 字形行数据
  __int16 v18;                  // ax: 处理后的行数据
  char v19;                     // t0: 临时变量
  char n16_2;                   // cl: 宽度计数器
  _BOOL1 v21;                   // cf: 进位标志
  char *n658255_3;              // [esp-24h] [ebp-24h]: 屏幕指针副本
  
  FDOTHER_DAT__1 = _FDOTHER.DAT_;
  n10_2 = n10;
  ::n658255 = n658255;
  ::argC = argC;
  ::arg10 = arg10;
  ::arg14 = arg14;
  arg18_1 = arg18;
  ::arg18 = arg18;
  
  // 如果flags非零, 先填充背景区域 (16x16)
  if ( (_BYTE)arg18 )
  {
    BYTE1(arg18_1) = arg18;
    arg18_2 = arg18_1;
    value = arg18_1 << 16;
    LOWORD(value) = arg18_2;
    n658255_1 = (char *)::n658255;
    n16 = 16;
    
    do
    {
      memset32(n658255_1, value, 4u);  // 填充16字节 (4个DWORD)
      n658255_1 += argC;
      --n16;
    }
    while ( n16 );
  }
  
  // 渲染字符 (如果不是空格)
  if ( n10_2 != 10 )            // 10可能是空格索引
  {
    // 获取字形数据 (每个字符32字节)
    v12 = (__int16 *)(32 * n10_2 + FDOTHER_DAT__1);
    n658255_2 = (char *)::n658255;
    arg10_1 = ::arg10;
    arg14_1 = ::arg14;
    n16_1 = 16;                 // 16行
    
    do
    {
      v17 = *v12++;             // 获取一行字形数据 (16位)
      v19 = v17;
      
      // 字节交换 (高字节<->低字节)
      LOBYTE(v18) = HIBYTE(v17);
      HIBYTE(v18) = v19;
      
      n658255_3 = n658255_2;
      n16_2 = 16;               // 16列
      
      do
      {
        // 左移并检查进位 (测试最高位)
        v21 = __CFSHL__(v18, 1);
        v18 *= 2;
        
        if ( v21 )              // 如果位为1
        {
          *n658255_2 = arg10_1;         // 前景色
          n658255_2[argC - 1] = arg14_1;  // 右边缘背景色
          n658255_2[argC] = arg14_1;      // 下一行背景色
        }
        ++n658255_2;
        --n16_2;
      }
      while ( n16_2 );
      
      n658255_2 = &n658255_3[argC];  // 下一行
      --n16_1;
    }
    while ( n16_1 );
  }
}
```

**工作原理**:
1. 从FDOTHER.DAT加载字形数据 (索引n10, 32字节/字符)
2. 每个字符16x16像素
3. 每行16位, 每位对应一个像素
4. 位为1: 使用前景色 (arg10)
5. 位为0: 使用背景色或透明 (arg14)
6. 支持背景填充和边缘处理

---

### 10. sub_11D40() - VGA调色板设置

**地址**: 0x11D40  
**大小**: 0xB2 (178字节)

**功能**: 通过VGA DAC端口设置调色板颜色

```c
void __fastcall sub_11D40(
    __int32 a1,     // eax: 参数
    int a2, a3, a4, // 其他参数
    int a5,         // 起始颜色索引 (0-255)
    int a6,         // 结束颜色索引
    int a7)         // 颜色偏移量
{
  int v8;                      // eax: 红色分量
  int v9;                      // eax: 绿色分量
  int v10;                     // eax: 蓝色分量
  
  sub_3702F(a1, a2, a3, a4, 24);
  
  // 遍历颜色索引范围
  while ( a5 <= a6 )
  {
    // 设置DAC写地址寄存器 (端口0x3C8 = 968)
    outp(968, a5);
    
    // 读取并调整红色分量
    v8 = *(unsigned __int8 *)(FDOTHER_DAT + 3 * a5) - a7;
    if ( v8 < 0 )
      LOBYTE(v8) = 0;
    outp(969, v8);             // 写入红色到DAC (端口0x3C9 = 969)
    
    // 读取并调整绿色分量
    v9 = *(unsigned __int8 *)(FDOTHER_DAT + 3 * a5 + 1) - a7;
    if ( v9 < 0 )
      LOBYTE(v9) = 0;
    outp(969, v9);             // 写入绿色
    
    // 读取并调整蓝色分量
    v10 = *(unsigned __int8 *)(FDOTHER_DAT + 3 * a5 + 2) - a7;
    if ( v10 < 0 )
      LOBYTE(v10) = 0;
    outp(969, v10);            // 写入蓝色
    
    ++a5;
  }
  
  JUMPOUT(0x1951F);
}
```

**VGA DAC端口说明**:
- **端口968 (0x3C8)**: DAC写地址寄存器 - 设置要修改的颜色索引
- **端口969 (0x3C9)**: DAC颜色数据寄存器 - 依次写入R, G, B分量

**颜色数据存储**:
- FDOTHER.DAT中每颜色占用3个连续字节
- 格式: [R, G, B, R, G, B, ...]
- 每个分量0-63 (6位)

**用途**:
- 设置游戏调色板
- 实现淡入淡出效果 (通过调整a7偏移量)
- 动态颜色动画

---

### 11. sub_19953() - 场景主渲染循环

**地址**: 0x19953  
**大小**: 0x4A4 (1188字节)

**功能**: 场景主渲染循环,包含动画、双缓冲和输入处理

```c
void __fastcall sub_19953(__int32 a1, int a2, int a3, int a4)
{
  int v4;                      // eax
  int n30;                     // ebx: 常量30
  int v6;                      // ebp: 动画计时器
  char *v7;                    // edi: 颜色数据指针
  __int64 n2_1;                // rax
  int n200;                    // esi: 行计数器
  int n86;                     // esi: 行计数器2
  int n2;                      // esi
  int v12;                     // edx
  __int16 *v14;                // eax
  int n86_1;                   // esi
  int n200_1;                  // esi
  int n4_1;                    // esi
  int v18;                     // eax
  int v19;                     // ebx
  _DWORD v20[2];               // [esp+0h] [ebp-28h]: 颜色索引表
  int v21;                     // [esp+8h] [ebp-20h]
  int v22;                     // [esp+Ch] [ebp-1Ch]
  int n4;                      // [esp+10h] [ebp-18h]: 层计数器
  char v24;                    // [esp+14h] [ebp-14h]: 动画状态
  int v25;                     // [esp+24h] [ebp-4h]
  
  sub_3702F(a1, a2, a3, a4, 68);
  v25 = a3;
  v20[0] = unk_51EED;          // 颜色索引1
  v20[1] = unk_51EF1;          // 颜色索引2
  v24 = 0;                     // 动画状态
  ::n4_1 = 0;
  
  // 随机初始值
  LOWORD(v4) = sub_4EBE3();
  n30 = 30;
  v6 = v4 % 30 + 2;            // 动画计时器 (2-31)
  v7 = (char *)&loc_1A599 + n655360 + 3;
  
  // 1. 复制后备缓冲到主缓冲
  n2_1 = memmove(n655360_0, 655360, 64000);
  v21 = 0;
  v22 = 0;
  
  // 2. 格式转换: 320字节/行 → 456字节/行
  for ( n200 = 0; n200 < 200; ++n200 )
    n2_1 = memmove(456 * (n200 - 4) + n655360 + 32900, 
                   &n655360_0[320 * n200], 320);
  
  // 3. 场景模式>1时的特殊处理
  if ( (unsigned int)FDFIELD_DAT__0 > 1 )
  {
    sub_1297D(n2_1, SHIDWORD(n2_1), 30, a4);
    sub_11EEE(n655360 + 32904, SHIDWORD(n2_1), 30, a4, 
              n655360 + 32904, 456, 13, 8, qword_53AA9, SHIDWORD(qword_53AA9));
    sub_127A9(n2_1, SHIDWORD(n2_1), 30, a4);
  }
  
  // 4. 处理4层叠加效果
  for ( n4 = 0; n4 < 4; ++n4 )
  {
    n86 = 0;
    v21 -= 4;
    v22 += 4;
    
    // 处理86行数据
    while ( n86 < 86 )
    {
      n30 = (int)n655360_0;
      n2_1 = memmove(456 * (n86 + 108) + n655360 + 32905, 
                     &n655360_0[320 * n86 + 35845], 310);
      ++n86;
    }
    
    // 叠加2层颜色
    for ( n2 = 0; n2 < 2; ++n2 )
    {
      n30 = FDOTHER_DAT__4;
      HIDWORD(n2_1) = FDOTHER_DAT__4 + *(_DWORD *)(12 * v20[n2] + FDOTHER_DAT__4);
      sub_4ED34((int)&v7[*(&v21 + n2)], SHIDWORD(n2_1), 456);
    }
    
    // 混合处理
    LODWORD(n2_1) = sub_11EB0(n655360 + 32904, SHIDWORD(n2_1), n30, a4, 
                              656644, 320, n655360 + 32904, 456, 312, 192);
  }
  
  // 5. 主渲染循环
  while ( 1 )
  {
    // 等待垂直同步 (同时检查键盘输入)
    while ( !sub_10620(n2_1, SHIDWORD(n2_1), n30, a4) )
    {
      sub_4E31C();
      LODWORD(n2_1) = MEMORY[0x46C] - dword_53C17;
      
      // 每2个滴答更新一次
      if ( (unsigned int)n2_1 >= 2 )
      {
        if ( ++dword_53C13 == 4 )
          dword_53C13 = 0;
        dword_53C17 = MEMORY[0x46C];
        
        // 更新场景渲染
        if ( (unsigned int)FDFIELD_DAT__0 > 1 )
        {
          sub_1297D(MEMORY[0x46C], SHIDWORD(n2_1), n30, a4);
          sub_11EEE(n655360 + 32904, SHIDWORD(n2_1), n30, a4, 
                    n655360 + 32904, 456, 13, 8, 
                    qword_53AA9, SHIDWORD(qword_53AA9));
          sub_127A9(n2_1, SHIDWORD(n2_1), n30, a4);
        }
        
        // 更新动画帧
        if ( v24 )
        {
          v12 = *(unsigned __int8 *)DATO_DAT;
          
          if ( FDFIELD_DAT__0 )
            sub_4EC31(&n655360_0[n1832], (__int16 *)(v12 + DATO_DAT), 320);
          else
            sub_4EBFF(&n655360_0[n1832], (__int16 *)(v12 + DATO_DAT), 320);
          
          LOWORD(n2_1) = sub_4EBE3();
          n30 = 30;
          HIDWORD(n2_1) = (int)n2_1 % 30;
          v6 = (int)n2_1 % 30 + 10;
          v24 = 0;
        }
        else if ( !v6-- )
        {
          v14 = (__int16 *)(*(_DWORD *)(DATO_DAT + 12) + DATO_DAT);
          
          if ( FDFIELD_DAT__0 )
            sub_4EC31(&n655360_0[n1832], v14, 320);
          else
            sub_4EBFF(&n655360_0[n1832], v14, 320);
          
          v24 = 1;
        }
        
        // 重新处理200行数据
        if ( (unsigned int)FDFIELD_DAT__0 <= 1 )
        {
          for ( n200_1 = 0; n200_1 < 200; ++n200_1 )
            n2_1 = memmove(456 * (n200_1 - 4) + n655360 + 32900, 
                           &n655360_0[320 * n200_1], 320);
        }
        else
        {
          for ( n86_1 = 0; n86_1 < 86; ++n86_1 )
            n2_1 = memmove(456 * (n86_1 + 108) + n655360 + 32905, 
                           &n655360_0[320 * n86_1 + 35845], 310);
        }
        
        // 叠加颜色层
        for ( n4_1 = 0; n4_1 < 2; ++n4_1 )
        {
          v19 = 3 * v20[n4_1];
          
          if ( (unsigned __int8 *)n4_1 == ::n4_1 )
          {
            HIDWORD(n2_1) = dword_53C13 >> 31;
            v19 += dword_53C13 / 2;
          }
          
          v18 = v19;
          n30 = FDOTHER_DAT__4;
          sub_4ED34((int)&v7[*(&v21 + n4_1)], 
                   FDOTHER_DAT__4 + *(_DWORD *)(FDOTHER_DAT__4 + 4 * v18), 456);
        }
        
        LODWORD(n2_1) = sub_11EB0(n655360 + 32904, SHIDWORD(n2_1), n30, a4, 
                                  656644, 320, n655360 + 32904, 456, 312, 192);
      }
    }
    
    // 6. 处理键盘输入
    HIBYTE(n3) = 16;
    int386(22, &n3, &n3);      // DOS键盘中断
    LODWORD(n2_1) = HIBYTE(n3);
    
    // 检查退出键
    if ( HIBYTE(n3) == 224 || HIBYTE(n3) == 82 || 
         HIBYTE(n3) == 28 || HIBYTE(n3) == 57 )
      break;
    
    switch ( HIBYTE(n3) )
    {
      case 1u:                 // ESC
      case 0x53u:              // Delete
        dword_53C13 = 0;
LABEL_57:
        JUMPOUT(0x13FCC);      // 退出
      case 0x4Bu:              // 左方向键
        ::n4_1 = 0;
        break;
      case 0x4Du:              // 右方向键
        ::n4_1 = (unsigned __int8 *)1;
        break;
    }
  }
  
  dword_53C13 = 0;
  goto LABEL_57;
}
```

---

### 12. sub_2670E() - 场景特效

**地址**: 0x2670E  
**大小**: 0x288 (648字节)

**功能**: 实现淡入淡出等过渡特效

```c
void __usercall sub_2670E(
    unsigned __int8 *a1@<edi>, 
    __int32 a2@<eax>, 
    int a3@<edx>, 
    int a4@<ecx>, 
    int a5@<ebx>)
{
  char *v6;                    // eax: 场景数据
  int v7;                      // eax
  int v8;                      // eax
  int v9;                      // eax
  int v10;                     // ebx
  int v11;                     // eax
  unsigned __int8 *n3;         // edi: 临时缓冲区
  __int64 v13;                 // rax
  int n10;                     // ebx: 渐变级别
  unsigned __int8 v15;         // [esp+0h] [ebp-14h]: 场景类型
  
  sub_3702F(a2, a3, a5, a4, 60);
  v6 = sub_4E809(n17);
  LOBYTE(v6) = *v6;
  v15 = (unsigned __int8)v6;
  sub_25977((__int32)v6, a3, a5, a4, -1, 0);
  
  // 场景类型2: 特殊处理
  if ( n5 == 2 )
  {
    v7 = sub_1956B(75);
    sub_15F84(a1, v7, a3, a4, a5, FDTXT_DAT__0, 513, 693535, 320, 205, 76, 74, 19, 1);
    FDFIELD_DAT__0 = 1;
    v8 = sub_16559(0);
    sub_19953(v8, a3, a5, a4);
    v10 = v9;
    sub_197E5();
    FDFIELD_DAT__0 = 0;
    sub_26996();
    
    if ( v10 == -1 || n4_1 )
      goto LABEL_23;
    
    // 检查场景边界
    if ( n17 < 27 && n16_1 > 16 || n17 > 26 && n16_1 > 20 )
    {
      n8_1 = n8_3;
      v11 = sub_2AF28();
      n8_1 = 0;
      if ( !v11 )
LABEL_23:
        JUMPOUT(0x29867);
    }
  }
  
  // 分配临时缓冲区 (64000字节)
  n8_1 = n8_3;
  n3 = (unsigned __int8 *)malloc(64000);
  v13 = memmove(n3, 655360, 64000);
  
  // 10级渐变效果
  for ( n10 = 1; n10 <= 10; ++n10 )
  {
    a4 = 6 * v15 + n5;
    
    // 计算渐变位置
    sub_2921A(
      ((n10 * ((unsigned __int8)byte_52363[a4] - 150) / 10) << 7) + 20480,
      ((n10 * ((unsigned __int8)byte_52375[a4] - 100) / 10) << 7) + 12800,
      (int)n3,
      128 - 9 * n10);
    
    // 复制到显存
    v13 = memmove(655360, n655360, 64000);
    
    // 更新调色板 (4*n10偏移)
    sub_11D40(4 * n10, SHIDWORD(v13), n10, a4, 0, 255, 4 * n10);
  }
  
  // 最终调色板设置
  sub_11D40(v13, SHIDWORD(v13), n10, a4, 0, 255, 64);
  LODWORD(v13) = memset(n10, 655360, 0, 64000);
  
  // 根据场景类型执行不同操作
  if ( n5 )
  {
    if ( n5 == 4 )
    {
      sub_25977(v13, SHIDWORD(v13), n10, a4, 11, 0);
      sub_29DAA(n3);
    }
    else
    {
      if ( n5 == 2 )
      {
LABEL_22:
        free(n3);
        n8_1 = 0;
        goto LABEL_23;
      }
      
      if ( n5 == 3 )
        sub_25977(v13, SHIDWORD(v13), n10, a4, 15, 0);
      else
        sub_25977(v13, SHIDWORD(v13), n10, a4, 14, 0);
      
      sub_279BC((int)n3);
    }
  }
  else
  {
    sub_25977(v13, SHIDWORD(v13), n10, a4, 13, 0);
    LODWORD(v13) = sub_29300(n3);
  }
  
  sub_25977(v13, SHIDWORD(v13), n10, a4, 10, 0);
  goto LABEL_22;
}
```

**特效类型**:
- **n5 == 0**: 淡入效果
- **n5 == 2**: 场景切换
- **n5 == 3**: 特殊效果1
- **n5 == 4**: 特殊效果2
- **其他**: 默认效果

---

### 13. sub_22E5C() - 场景初始化

**地址**: 0x22E5C

**功能**: 初始化游戏场景 (状态1)

```c
void __fastcall sub_22E5C(__int32 a1, int a2, int a3, int a4)
{
  int v4;                      // eax
  __int32 v5;                  // eax
  int v6;                      // eax
  _BYTE *_FDOTHER.DAT_;        // ebx: 数据指针
  int v8;                      // eax
  int v9;                      // eax
  int v10;                     // eax
  
  sub_3702F(a1, a2, a3, a4, 28);
  
  // 设置游戏状态
  sub_25977(v4, a2, a3, a4, -1, 1);
  sub_17AA9(v5, a2, a3, a4, 1);
  
  v6 = sub_1F882();
  
  // 加载FDOTHER.DAT索引79
  _FDOTHER.DAT_ = sub_111BA(v6, a2, a3, a4, 
                             (int)"FDOTHER.DAT", 0, 79);
  
  // 清零缓冲区
  memset((int)_FDOTHER.DAT_, 655360, 0, 64000);
  
  // 处理数据 (偏移0)
  v8 = sub_2EB9F((int)_FDOTHER.DAT_, 0, 655360, 320, -1);
  v9 = sub_1F525(v8, a2, (int)_FDOTHER.DAT_, a4);
  sub_17AA9(v9, a2, (int)_FDOTHER.DAT_, a4, 9);
  
  // 处理数据 (偏移1)
  v10 = sub_2EB9F((int)_FDOTHER.DAT_, 1, 655360, 320, -1);
  sub_17AA9(v10, a2, (int)_FDOTHER.DAT_, a4, 36);
  
  JUMPOUT(0x15E94);
}
```

---

## 📊 函数汇总表

### 游戏逻辑函数 (13个)

| 序号 | 地址 | 函数名 | 大小 | 功能 |
|------|------|--------|------|------|
| 1 | 0x3CCB4 | start | 0x268 | 程序入口,初始化DOS环境 |
| 2 | 0x25BF4 | main | 0x2C7 | 游戏主循环 |
| 3 | 0x25EBB | sub_25EBB | 0x297 | 游戏状态处理和场景加载 |
| 4 | 0x22E5C | sub_22E5C | - | 场景初始化 (状态1) |
| 5 | 0x117E7 | sub_117E7 | 0x2C1 | 键盘输入处理 |
| 6 | 0x26152 | sub_26152 | 0x49A | 场景主逻辑处理 |
| 7 | 0x2670E | sub_2670E | 0x288 | 场景特效和过渡效果 |
| 8 | 0x190AC | sub_190AC | 0x4BF | 场景交互和绘制 |
| 9 | 0x111BA | sub_111BA | 0xEB | 数据文件加载 |
| 10 | 0x25977 | sub_25977 | - | 游戏状态设置 |
| 11 | 0x11AA8 | sub_11AA8 | 0xA0 | 获取按键扫描码 |
| 12 | 0x12C0D | sub_12C0D | 0x53 | 获取当前选择项 |
| 13 | 0x17AED | sub_17AED | 0x282 | 执行交互动作 |

### 绘制框架函数 (13个)

| 序号 | 地址 | 函数名 | 大小 | 功能 |
|------|------|--------|------|------|
| 1 | 0x15F84 | sub_15F84 | 0x564 | 文本/UI渲染引擎 |
| 2 | 0x4EBFF | sub_4EBFF | - | 正向位块传输 (BitBlt) |
| 3 | 0x4EC31 | sub_4EC31 | - | 反向位块传输 (镜像) |
| 4 | 0x4ED7A | sub_4ED7A | - | 字符/精灵渲染 (16x16) |
| 5 | 0x4EC66 | sub_4EC66 | - | 获取下一个像素值 |
| 6 | 0x11D40 | sub_11D40 | 0xB2 | VGA调色板设置 |
| 7 | 0x19953 | sub_19953 | 0x4A4 | 场景主渲染循环 |
| 8 | 0x2670E | sub_2670E | 0x288 | 场景特效和过渡 |
| 9 | 0x190AC | sub_190AC | 0x4BF | 场景交互和绘制 |
| 10 | 0x165AC | sub_165AC | 0x2B0 | 精灵加载/处理 |
| 11 | 0x16B43 | sub_16B43 | 0x114 | 精灵释放 |
| 12 | 0x16C57 | sub_16C57 | 0x1CD | 精灵渲染 |
| 13 | 0x10620 | sub_10620 | 0x32 | 垂直同步等待 |

---

## 🔧 关键技术说明

### 1. DOS中断调用

```c
// BIOS视频中断 (设置显示模式)
int386(16, &n3, &n3);
// n3=19: VGA模式13h (320x200 256色)
// n3=3: 文本模式

// DOS键盘中断 (获取按键)
int386(22, &n3, &n3);
// 返回扫描码在HIBYTE(n3)
```

### 2. VGA端口操作

```c
// 设置调色板
outp(968, colorIndex);   // DAC写地址寄存器
outp(969, red);          // 红色分量
outp(969, green);        // 绿色分量
outp(969, blue);         // 蓝色分量
```

### 3. 显存布局

```
VGA显存: 0xA0000 (655360十进制)
大小: 64000字节 (320x200)
格式: 每字节一个像素索引 (0-255)
```

### 4. 定时器系统

```c
// DOS定时器滴答计数器
MEMORY[0x46C]  // 每秒约18.2个滴答

// 使用示例
dword_53C17 = MEMORY[0x46C];  // 保存当前值
if ((MEMORY[0x46C] - dword_53C17) >= 2) {
    // 更新动画
}
```

---

## 📝 更新记录

| 日期 | 内容 |
|------|------|
| 2026-05-04 | 初始分析,完成13个游戏逻辑函数和13个绘制函数的反编译 |

---

**分析工具**: IDA Pro MCP Server  
**文件路径**: D:\workspace\fd2ida\FD2\FD2.EXE  
**分析日期**: 2026-05-04
